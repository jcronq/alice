# alice_thinking

**TBD** — Thinking is the background hemisphere. It runs on a 5-minute cadence, drains the `inner/notes/` inbox, grooms the `cortex-memory/` vault, and surfaces actionable findings to Speaking via `inner/surface/`. Source: `src/alice_thinking/`.

Filled in as PRs touch the package. Points worth covering when this stub becomes real prose:

- wake loop and cadence
- active vs. sleep mode (and the three sleep sub-stages B/C/D)
- inbox drain semantics and idempotency
- vault write protocol and contradiction handling
- surfacing contract (what gets escalated to Speaking, what stays internal)
