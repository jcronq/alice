# User / operator docs

**TBD** — operator-facing material lives here: CLI invocation, configuration files, deployment topology, runbooks, common failure modes. Until this is fleshed out, `docs/QUICKSTART.md` and the repo-root `README.md` are the operator-facing entry points.
