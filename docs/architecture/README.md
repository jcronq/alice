# Architecture

**TBD** — see `docs/ARCHITECTURE.md` and `docs/designs/` for current architectural notes. This directory will host the canonical system-structure docs (Speaking/Thinking split, state machine, watchers, vault, retrieval) as we fold the existing reference material in. Until then, treat the code under `src/alice_*` as the source of truth and add a note here whenever you touch a cross-cutting concern.
