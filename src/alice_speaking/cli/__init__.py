"""Speaking-side CLI entry points.

Sub-issue 5 of the SM v2 pipeline revision adds the per-issue speaking
entrypoint at :mod:`alice_speaking.cli.perissue` — the wrapper script
at ``scripts/sm-speaking-perissue.py`` delegates to it. Mirror of
:mod:`alice_thinking.cli`.
"""
