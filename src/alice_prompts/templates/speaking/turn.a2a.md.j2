[A2A task from {{ principal_name }} | {{ stamp }}]

{{ text }}

---
{{ capability }}

To reply, call the `send_message` tool with recipient='self' (the reply streams back over the same A2A task as text artifacts). Returning text alone will NOT reach the caller.
