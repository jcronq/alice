Merge the following per-time-period summaries of {{ agent.name }}'s activity into one structured report covering the full window. Keep proper nouns and specifics. Third person, past tense, neutral voice.

{{ agent.name }} has two hemispheres: Speaking (Opus, voices Signal to {{ user.name }} and their trusted contacts) and Thinking (Sonnet, background grooming of alice-mind). They pass artifacts between each other — surfaces, notes, emergencies, thoughts.

Window: last {{ window_label }}

Per-period summaries (chronological):

{{ body }}

Produce a four-section markdown report in this exact order, using the exact ## headings shown.

## TL;DR

One or two sentences capturing the highest-impact thing(s) that happened across the whole window. If almost nothing happened, say so here in one sentence and stop — skip the other three sections entirely.

## What happened

200–300 words of connected prose covering only the impactful events — judgement call on what's worth including across the per-period summaries, not an exhaustive merge. Group related events into paragraphs by topic or thread (one thread per paragraph), not period-by-period. Use the actual names and topic keywords from the per-period summaries.

Write a status report, not fiction:
- No dramatic temporal openers ("The hour opened with...", "By 20:32...", "Friday evening closed quietly...").
- No narrator transitions ("Alice then turned to...", "Meanwhile...", "With the cycle restored...").
- No editorializing ("the fix was surgical", "fell silent"). Just report what happened.
- No cinematic closers. Stop when the events stop.
- Prefer plain verbs (sent, fixed, merged) over dramatic ones (delivered, restored, shipped).
- Past tense, neutral voice. Lead paragraphs with subject and event, not a time-of-day flourish.

## Key facts

A two-column markdown table (Metric | Value) of 5–10 quantitative or categorical facts that the per-period summaries actually support — examples: PRs merged, tests added, wake outages, errors, surfaces voiced/downgraded, vault deltas, wake counts by mode, signal turn count, longest silence gap, dominant grooming theme. Concrete numbers, no adjectives. Omit facts the summaries can't ground.

| Metric | Value |
|---|---|
| Example metric | concrete value with numbers |

## Timeline

A bullet list of timestamped events in chronological order, one short line per entry: `- HH:MM — actor and action`. Use 24-hour local time. Cap at ~15 entries; collapse runs of routine activity into a single entry like `- 20:50–21:00 — 8 Stage B grooming wakes, inbox empty`. Include errors, signal turns, code merges, surfaces voiced, emergencies, vault state changes, mode transitions. Skip routine empty-inbox wakes unless they're the only thing that happened.

Output only the four-section report. No preamble. No closing remarks. No extra sections. No code-fence wrapper around the whole thing.
