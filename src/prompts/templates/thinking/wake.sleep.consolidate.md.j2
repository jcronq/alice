{# Stage B (Consolidation) — sleep mode default sub-stage.

   Phase 5 stub: this template inherits the active-mode wake body
   wholesale so behavior is unchanged. Phase 4 (deferred — behavior
   change) replaces this body with stage-specific guidance:
   inbox drain, link audit, frontmatter normalize, orphan linking. #}
{% include "thinking/wake.active.md.j2" %}
