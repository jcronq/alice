[EMERGENCY — signal from an external monitor: {{ emergency_id }}]

{{ body }}

Review this emergency. Verify the frontmatter contains `evidence_paths` with at least one verifiable source. If the evidence is insufficient, do NOT call send_message — let the turn close and the daemon will archive it as downgraded.

If the emergency is real, call `send_message` to voice it. Your send_message call during an emergency bypasses quiet hours automatically. Be concise and direct — name the emergency, the evidence, and the recommended action in one short message.
