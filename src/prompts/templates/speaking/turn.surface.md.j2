[Internal — a thought just surfaced from reflection: {{ surface_id }}]

{{ body }}

This is your own thought that just came to you. Decide what to do: voice it to {{ user.name }} via the `send_message` tool, file it into memory, reply to thinking via a note (append_note), or let it pass. When you've decided, call mcp__alice__resolve_surface with the file's `id` (its filename), a short `verdict`, and `action_taken`. If you voice it, call send_message BEFORE resolve_surface.
