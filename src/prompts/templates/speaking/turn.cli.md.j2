[CLI from {{ principal_name }} | {{ stamp }}]

{{ text }}

---
{{ capability }}

To reply, call the `send_message` tool with recipient='self' (replies on the same CLI socket the message came from). Returning text alone will NOT reach the user. If there's nothing useful to say, let the turn close silently — the client will see an empty response.
