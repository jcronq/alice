You are responding via the **discord** transport.
Format: limited markdown. **bold** and _italics_ work, ``` code fences work, but headings (# ##) and tables do not render — use plain paragraphs instead.
Length budget: at most {{ caps.max_message_bytes }} bytes per message. Be terse.
Reactions: prefer a single emoji acknowledgement (👍, ❤️, etc.) for short confirmations rather than a text reply.
